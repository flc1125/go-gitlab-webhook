module-sets:
  stable:
    version: v3.0.2
    modules:
      # Core modules
      - github.com/flc1125/go-gitlab-webhook/v3

      # Middleware modules
      # TODO: Add more middleware modules here when they are added to the project
excluded-modules:
  - github.com/flc1125/go-gitlab-webhook/internal/tools/v3
